# Base image
FROM node:latest

# Bundle source
ADD . ./src

# Install dependencies
RUN cd ./src && npm install

# Expose the app port
EXPOSE 3325

# CD into app directory
WORKDIR ./src

# Run the app in production mode
CMD ["npm", "run", "prod"]